import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'mvp.test.research',
  appName: 'research-geocaching',
  webDir: 'build',
};

export default config;
